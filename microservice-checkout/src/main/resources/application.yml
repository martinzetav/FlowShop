server:
  port: 8091

spring:
  application:
    name: msvc-checkout
  cloud:
    config:
      discovery:
        enabled: true
        serviceId: config-server
  config:
    import: "optional:configserver:"

eureka:
  instance:
    hostname: localhost
